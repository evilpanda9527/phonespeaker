**Prompt(收尾第二批:打包 + GitHub 門面 — 先出計畫,確認後再實作)**

> 第一批(WiFi 提示 + 雙語)已通過驗收。現在進第二批:打包成可發佈的成品 + GitHub public 門面。**請先出一份打包計畫給我確認,列出工具、產出物、步驟、風險,先不要實作。** 我確認後才動手,且預期會分多輪(打包需實機反覆驗證)。
>
> **最終定位提醒:** 開源、放 GitHub public 供人下載自用、自簽、不上架。傳輸 = WiFi + USB(網路共享)+ USB(adb)。**不含 BT / U3 / 任何 WinUSB 驅動**。
>
> **要交付的東西:**
>
> **A. PC 端打包**
> 1. PyInstaller 把 `pc/` 打包成 exe(使用者免裝 Python)。注意處理:資源/資料檔路徑(打包後路徑跟開發時不同)、customtkinter 等相依是否需額外收集。
> 2. **adb 隨附**:目前 U2 依賴系統 adb。打包後要改成用「隨附的 adb」(放進發佈包、程式以相對路徑找它),**不再依賴使用者機器上的 adb / PATH**。授權:adb 是 Google 的,隨附需符合其授權,請確認並在文件註明。
> 3. Inno Setup(或 NSIS)做**單一安裝檔**:一次裝好 exe + adb + 捷徑。首次執行的 Windows 防火牆允許(TCP 58482)要能正常提示。
>
> **B. Android 端打包**
> 4. build **release APK**(自簽即可)。放到明確輸出位置。
>
> **C. GitHub public 門面**
> 5. **README(繁中 + 英文雙語)**,含:專案簡介、三種傳輸的使用方式、**詳細安裝步驟**、以及**必須說清楚的使用者會遇到的狀況**——(a) Windows SmartScreen/防毒可能誤報自簽 exe、怎麼繞;(b) APK 是自簽、安裝時要允許「未知來源/不受信任」、怎麼操作;(c) USB 網路共享 / USB 偵錯怎麼開;(d) 防火牆放行;(e) 三傳輸各自前提(WiFi 同網段 / USB 網路共享 / USB 偵錯)。最好有截圖位置預留。
> 6. **LICENSE**:選一個適合開源自用專案的授權(例如 MIT),你建議並說明。
> 7. **隱私/責任聲明**:說明本 app 擷取 PC 系統音訊、只在區域網路/USB 內傳輸、**不上傳任何資料到任何伺服器**,讓下載者安心。
> 8. README 註明**「使用 Claude Code 開發」**。
> 9. 確認 `.gitignore` 正確(不夾帶 .env、憑證、build 中間產物、大型二進位如不該進 repo 的話);原始碼要完整公開可 build。
>
> **約束:**
> - 打包是「包裝」,**不得修改任何已驗收的功能程式碼**(transport/協定/播放/停止/雙語)。若打包過程發現非改不可(例如 exe 資源路徑、adb 路徑解析),**先跟我說明再改**,不要默默動功能碼。
> - 診斷用 `[停止診斷]` log:發佈版建議降級或收乾淨(避免正式使用者看到雜訊),你建議怎麼處理。
> - 依 §10.4 回報。
>
> **驗證方式(我會實機做,你把步驟寫清楚):**
> - 我會**實際用安裝檔在 Windows 裝一次**,測 WiFi/U1/U2 三傳輸都正常;裝 APK 到手機測。請預期我可能回報「乾淨環境的打包問題」(缺 DLL、adb 找不到、防毒/防火牆等),需要來回修。
>
> **先給計畫,等我確認再實作。**

---
**Prompt(§16-3:zeroconf 綁定正確網卡)**

> 請處理 SPEC3.md §16 第 3 項,**只做這一項**。
>
> **背景(實測)**:PC 端 zeroconf 廣播服務時,挑到虛擬網卡 IP(VirtualBox 的 `192.168.56.1`)對外宣告,而非真正連 WiFi 的網卡(`192.168.18.x`),導致手機連到錯誤網段、連線失敗。目前靠「手動停用虛擬網卡 + WLAN 設私人網路」繞過。
>
> **要求:**
> 1. PC 端 zeroconf 廣播/綁定時,自動選對外可達的實體網卡 IP(或排除 `192.168.56.x`、`169.254.x` 這類虛擬/link-local 網段),讓使用者**不必手動停用虛擬網卡**也能被手機正確探索連上。
> 2. **重要相容性**:實測已知 USB tethering 下 mDNS 能跨 RNDIS 連上(PC `10.132.200.x`,介面 nice_name 含 `Remote NDIS`)。你的網卡選擇/排除邏輯**不可誤擋掉這張 tethering 介面**——之後 U1 會用到它。也就是「排除虛擬/link-local」但「保留實體 WiFi 與 RNDIS tethering」。
> 3. 若同時有多張有效實體網卡,採合理策略(例如宣告作業系統預設路由對應那張、或宣告多個可達位址讓手機自選);在回報中說明你採用的策略與理由。
> 4. **修改僅限 PC 端 WiFi transport / zeroconf 廣播邏輯**,不動協定層、Android 端、AudioPlayer、mute_control。
> 5. 依 §10.4 回報改動範圍。
> 6. 驗證方式:請告訴我如何驗證——我會**把之前停用的虛擬網卡重新啟用、WLAN 恢復原設定**(還原成問題環境),確認手機仍能自動連到正確的 WiFi 網段。

---
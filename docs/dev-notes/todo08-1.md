請在 PC 端(和手機端,如適用)加入「選擇 transport 時提示使用者開啟對應開關」的引導,**只做這個 UI 提示,不動任何連線/傳輸邏輯**:

1. 當使用者選「**USB (USB 網路共享)**」→ 顯示提示:請先在手機開啟「USB 網路共享 / USB tethering」。
2. 當使用者選「**USB (adb)**」→ 顯示提示:請先在手機開啟「USB 偵錯」(開發者選項)。
3. WiFi 則不需要額外提示(維持現狀)。

**要求:**
- 提示呈現方式你判斷最合適的即可(例如:下拉選到該項時,在下方顯示一行說明文字;或啟動時若偵測不到對應條件才提示)。**以清楚、不干擾為原則**,不要用會擋住流程的強制彈窗。
- **這是純 UI/提示層改動,不得修改**任何 transport 的連線邏輯、協定層、AudioPlayer、停止/生命週期、ring buffer。
- 先 `git commit` 現況再改。
- 依 §10.4 回報改了哪些檔案。
- 完成後告訴我怎麼驗證(選不同 transport 時提示是否正確顯示)。

---
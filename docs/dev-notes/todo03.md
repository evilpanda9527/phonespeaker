**Prompt ②(①測過後再做):zeroconf 綁定正確網卡**

> 請處理 SPEC3.md §16 第 3 項,**只做這一項,不要動其他**。
>
> **背景(實測現象)**:PC 端 zeroconf 廣播服務時,挑到虛擬網卡 IP(VirtualBox 的 `192.168.56.1`)對外宣告,而非真正連 WiFi 的網卡(`192.168.18.x`),導致手機連到錯誤網段、連線失敗。目前靠「手動停用虛擬網卡 + WLAN 設為私人網路」暫時繞過。
>
> **要求**:
> 1. PC 端 zeroconf 廣播/綁定時,應選對外可達的實體網卡 IP(或排除 `192.168.56.x` 這類虛擬/host-only 網段),讓使用者**不必手動停用虛擬網卡**也能被手機正確探索連上。
> 2. 若同時存在多張有效網卡,採合理策略(例如宣告預設路由對應的那張,或宣告多個位址讓手機自行選可達的);請在回報中說明你採用的策略與理由。
> 3. **修改僅限 PC 端 WiFi transport / zeroconf 廣播邏輯**,不得動協定層、Android 端、`AudioPlayer`、`mute_control`。
> 4. 依 §10.4 回報改動範圍,並確認未動到已通過驗收的部分。
> 5. 修改後告訴我如何驗證:**把虛擬網卡重新啟用**(還原成問題環境),確認手機仍能連到正確的 `192.168.18.x`。

---

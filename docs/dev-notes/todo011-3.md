> 我核對了你提供的診斷 log(10:07:14 到 10:07:26 這段),發現一個你的鎖修法沒有涵蓋到的根本問題:**輪詢執行緒(`probe_state()`)每隔約 2.3 秒就重新對 adb 發起一次獨立呼叫**,整段期間 `adb server 監聽中` 一直是 `True`、`_adb_poll_owns_server=False`(這個 server 從頭到尾都不是我們啟動的)。
>
> 這代表:即使加了鎖解決「同時執行互撞」,**只要輪詢還是每 2.3 秒重複呼叫一次 adb,就會持續製造大量「可能跟 connect()/disconnect() 相撞」的機會窗口**,鎖只是讓大家排隊、不會減少相撞的根本頻率。
>
> **請一併檢討:**
> 1. 這個輪詢是否真的需要每 2.3 秒持續呼叫?如果 `adb server 監聽中` 這種狀態不常變化,是否可以**降低輪詢頻率**,或**在確認狀態穩定後就停止重複輪詢**(例如偵測到已連線就停止背景輪詢,只在真正需要重新確認時才查一次)?
> 2. 是否可以**減少總體的 adb 呼叫次數**,而不只是靠鎖把大家排隊——次數越少,即使沒有鎖,相撞機率也天然降低。
>
> 請連同鎖修法一起,重新評估輪詢機制本身的必要性和頻率,不要只加鎖就視為解決,依 §10.4 提出完整方案再讓我確認。

---
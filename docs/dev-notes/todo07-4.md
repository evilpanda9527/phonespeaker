你的順序和決定我都收到了(先修 WiFi 雜音 → U2 → U3 → BT)。但第 3 題你問到一個**非常關鍵、而且我很高興你問**的問題——因為這暴露了我之前可能把你導向一個「重造輪子」的方向。我得誠實重講。

## 為什麼真 BT Speaker 沒有頻寬問題?因為它用了硬體專用的音訊管線 + 壓縮

真藍牙喇叭之所以能傳 CD 音質,不是因為藍牙頻寬大,而是因為:

1. **它一定有壓縮。** A2DP(藍牙音訊的標準協定)從來不傳無損 PCM,它一定經過 SBC / AAC / aptX / LDAC 這些**編碼壓縮**過。SBC 壓完大概 300~350 kbps,塞進藍牙頻寬綽綽有餘。所以「藍牙喇叭沒頻寬問題」的真相是——**它壓縮了,只是你聽不出來。**

**Prompt(修 WiFi 雜音:疑似 underrun,調 ring buffer)**

> 請處理 WiFi 偶爾雜音問題。**已確認的關鍵事實:雜音只在 WiFi 出現,USB(有線)完全乾淨。** 這強烈指向 WiFi 網路抖動造成的 ring buffer underrun(供料偶爾不及),而非串流邏輯 bug。
>
> **要求:**
> 1. 針對 underrun 改善:適度**調大 ring buffer 的目標緩衝**(目前 §9 訂 target 20–60ms),用一點延遲換取 WiFi 下的穩定,減少 underrun 造成的雜音。可考慮讓 buffer 目標可依 transport 不同而不同(WiFi 需要較大、USB 可較小),但若那樣改動太大,先用一個對兩者都安全的折衷值即可。
> 2. 若 underrun 發生時目前是「補靜音/直接播不連續資料」,確認補償方式不會製造爆音(必要時補靜音而非跳接)。
> 3. **不可為了 WiFi 而讓 USB 的低延遲退化太多**——USB 目前無雜音、延遲好,盡量維持。
> 4. 修改僅限 PC/Android 的 ring buffer / 緩衝相關邏輯,**不動協定層、transport 連線邏輯、剛修好的停止/生命週期**。先 commit 現況再改。
> 5. 依 §10.4 回報改了哪些檔案、以及新的 buffer 目標值。
> 6. 告訴我如何驗證:WiFi 放一段長音樂確認雜音明顯減少/消失,USB 再測一次確認延遲沒明顯變差。
>
> 這是調校性質的改動,可能要來回試幾個 buffer 值,先給一個你認為合理的起點讓我測。

---
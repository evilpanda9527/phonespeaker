**Prompt(清除 PC 端測試殘留,準備乾淨重測)**

> 我要模擬「乾淨機器第一次安裝」來測試發佈包,請幫我清除 PC 端的測試殘留。**只清以下四樣,其他一律不准碰:**
>
> 1. **執行中的 PhoneSpeaker 程序**:若有正在跑的 `PhoneSpeaker.exe`,結束它。
> 2. **設定/log 殘留**:刪除 `%LOCALAPPDATA%\PhoneSpeaker` 整個資料夾(這是程式執行時寫的 log/config,清掉才能測乾淨的首次執行)。
> 3. **測試防火牆規則**:刪除防火牆規則 `PhoneSpeakerTestRule` 和 `PhoneSpeaker (TCP 58482)`(這兩條是之前測試留下的,清掉才能測「乾淨機器首次跑會不會跳防火牆」)。需要管理員權限的話,請告訴我、或給我指令我自己在管理員 PowerShell 跑。
> 4. **舊的解壓測試目錄**:若你之前解壓 portable zip 到某個臨時/測試資料夾,清掉那個解壓出來的資料夾。
>
> **絕對禁止(紅線):**
> - **不准刪除、移動、修改專案資料夾 `D:\Claude_Desktop_Test\PhoneSpeaker\` 本體**——裡面的原始碼、.git、SPEC、dist 裡的 zip/APK 產物、installer 等,一個都不准碰。
> - 不准動 git、不准 push、不准碰 GitHub。
> - `pc\dist\` 裡的 **portable zip 要保留**(我等下要用它重測),只清「解壓出來的測試副本」,不是 zip 本身。
>
> **每個刪除操作請讓我逐一確認(不要 bypass)。** 清完列出你清了哪四樣、確認專案本體未動。

---
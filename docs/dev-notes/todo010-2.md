**Prompt(改用最簡發佈方式:PC portable zip + Android debug APK)**

> 發佈方式改成最簡版本,理由:自用開源工具,不想處理 release 金鑰保管、也不想要安裝流程。**先 git commit & push to github 現況**,然後:
>
> **一、PC 端改用 portable zip(免安裝):**
> - `pc\dist\PhoneSpeaker\` 的 onedir 產物本身就是 portable。請提供一個打包成 zip 的方式(可以在 build.bat 尾端加一步、或另做一個小腳本),產出像 `PhoneSpeaker-PC-portable-v1.0.0.zip`。
> - **adb 要一起放進 zip**(放在 exe 旁的 `adb\` 子目錄,就是現在 usb_adb.py 會找的位置),這樣 U2 一樣免使用者自己裝 adb。
> - Inno Setup 安裝檔那套(installer/)**保留在專案裡不要刪**,但這次發佈不用它——當作「日後想做正式安裝版」的選項留著。
> - **防火牆**:portable 版沒有安裝檔自動放行,所以使用者第一次跑 WiFi/U1/U2 時 Windows 會彈防火牆詢問——這要在 README 說明「按允許」。
>
> **二、Android 端改用 debug APK(免金鑰):**
> - 發佈用 **debug APK**(`gradlew assembleDebug` 或 Android Studio Build → APK 的產物),使用者側載安裝。不需要 release 金鑰、不需要 keystore.properties。
> - build.gradle.kts 裡的 release 簽章設定**保留不要刪**(日後想出正式版的選項),這次發佈就是不走 release。
> - README 說明:debug APK 側載要「允許未知來源」;且註明「這是 debug 版、升級新版時可能需先解除安裝舊版」。
>
> **三、更新輸出與文件:**
> - 兩個發佈產物明確命名、放明確位置:PC 的 portable zip、Android 的 debug APK。
> - 確認 `.gitignore` 正確(zip 和 APK 不進 repo,之後手動附到 GitHub Release)。
>
> **約束:** 不動任何已驗收功能程式碼;installer/ 和 release 簽章設定都是「保留備用」,不刪不改。依 §10.4 回報。
>
> **完成後告訴我怎麼驗證:** 我會把 portable zip 解壓、雙擊 exe 測三種傳輸(留意防火牆首次詢問);把 debug APK 側載到手機測。
>
> 這批做完,下一批才寫 README/門面文件(照你之前的建議:文件要照我實測的真實流程寫)。

---
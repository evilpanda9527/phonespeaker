**Prompt(收尾第一批:WiFi 提示 + 繁中/英雙語)**

> 專案最終定位已確定,請先在 SPEC3.md 記錄一段「最終定位」再開工:**PhoneSpeaker 為開源專案,將放上 GitHub public 供人下載自用(不上架 Google Play,自簽)。傳輸方式最終為 WiFi + USB(網路共享)+ USB(adb) 三種;BT 不做(實機查證手機無可用 A2DP Sink)、USB U3 不做(Windows 11 libwdi 驅動 blocker)。介面支援繁體中文 + 英文。開發方式註明「使用 Claude Code 開發」。**
>
> 開工前先 `git commit`。本批**只做兩件事**,不碰打包(打包是下一批):
>
> **一、WiFi 選擇提示(比照現有 U1/U2 提示):**
> - 當使用者選「WiFi」→ 顯示提示:**PC 與手機需連線至同一個 WiFi 網路**。
> - 呈現方式與現有 U1/U2 的提示一致(同一種樣式、同一個機制),保持三種傳輸的提示風格統一。
> - 純 UI 提示,不動任何連線邏輯。
>
> **二、繁體中文 + 英文雙語(PC 端與 Android 端都要):**
> - 抽出介面上所有面向使用者的文字(按鈕、狀態、log 提示訊息、傳輸選項提示等),做成可切換的語言資源(zh-TW / en)。
> - **語言選擇:跟隨系統語言自動選(系統為中文→繁中,其餘→英文),同時在介面提供手動切換**(下拉或按鈕),讓使用者能覆寫。手動選擇要能記住(下次啟動沿用)。
> - 兩端(PC customtkinter / Android)各自實作,文字資源結構清楚、方便日後加語言。
> - 翻譯品質要通順、自然(尤其英文),不要生硬機翻感。
>
> **約束(§10):**
> - **不得修改任何已通過驗收的功能邏輯**(transport 連線、協定、播放、停止/生命週期、ring buffer)——本批只動 UI 文字層與提示。若某段文字目前寫死在功能程式碼裡、抽出時必須碰到該檔,先跟我說明再動。
> - 依 §10.4 回報改了哪些檔案。
>
> **驗證方式(請告訴我怎麼測):**
> 1. 選 WiFi 時出現「同一 WiFi」提示;U1/U2 提示仍正常。
> 2. 系統語言為中文時介面顯繁中、切到英文後全部正確變英文(反之亦然);手動切換有效、重啟後記住。
> 3. **回歸**:三種傳輸(WiFi/U1/U2)實際連線播放仍正常(確認抽文字沒把功能弄壞)。
>
> 完成後 `git commit`。這批過了再進第二批(打包 + GitHub 門面文件)。

---
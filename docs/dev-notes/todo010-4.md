**Prompt(修 bug:關閉 app 後殘留的 adb.exe 沒有跟著關閉，且有持續跳斷線警告)**

發現一個可重現的 bug,請查明並修正。

**現象(有截圖佐證):** 使用 U2「USB (adb)」後,即使已關閉 PhoneSpeaker PC app、拔除 USB,工作管理員裡仍殘留一個 `adb.exe` 在執行(實測 PID 17644、狀態「正在運行」)且**每隔一段時間跳出 Windows 警告**「該設備已停止響應或已斷開連接」(對已拔除的手機),關掉後過一陣子又跳，不確定原因是否與這個孤兒 adb.exe有關，但是我手動關閉adb.exe後還是有跳出警告，過了一段時間後好像就沒有出現了。

**可能原因判斷:** U2 模式會啟動 adb server(常駐背景 process)。目前關閉 app / 停止 U2 傳輸時,**沒有把 PhoneSpeaker 叫起來的那個 adb.exe 一併終止**,它變成孤兒繼續對已斷線的裝置輪詢、發警告。

**要求:**
1. 讓 U2 用到的 adb 生命週期跟著 PhoneSpeaker 走:**停止 U2 傳輸 / 關閉 app 時,要乾淨地結束由本程式啟動的 adb 相關 process**(例如適當時機呼叫 `adb kill-server`,或終止本程式啟動的那個 adb.exe),不要留下孤兒 process 持續跳警告。
2. 注意分寸:**只清理「本程式為了 U2 而啟動的 adb」**;如果使用者自己另外裝了 adb、或有其他程式在用 adb,盡量不要粗暴地把不屬於我們的 adb server 也殺掉(若技術上難以區分,請說明你的取捨,並以「不留孤兒警告」為優先,但在文件/log 註明會影響到系統 adb server)。
3. 不影響其他傳輸(WiFi/U1)——它們根本不該叫起 adb。
4. 這是收尾階段,請先 `git commit` 現況再改;修改儘量集中在 U2 相關的 `usb_adb.py` 與停止/生命週期收尾邏輯,不要動已驗收的協定/播放/其他 transport。若必須碰到共用的停止流程,依 §10.4 說明並確認不影響 WiFi/U1。
5. 檢查其他可能原因
6. 依 §10.4 回報改了哪些檔案。

**驗證方式(我會實測):** 用 U2 連線→播放→停止/關 app→拔 USB,然後**開工作管理員確認沒有殘留的 adb.exe、也不再跳「設備已停止響應」的警告**。同時確認 WiFi/U1 不受影響。

---
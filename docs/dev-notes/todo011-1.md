**Prompt(U2 二次啟動失敗:先加診斷 log,不要用猜的修)**

> 發現一個需要精確診斷的 bug,**這次請先加診斷 log、不要直接猜測修改**(比照 §16-4 停止逾時當初的做法)。
>
> **實測現象(可重現):**
> 1. App 第一次啟動,WiFi / U1 / U2 三種傳輸都正常連線。
> 2. 關閉 app(觀察到 adb.exe 有跟著正確關閉——這代表 todo010-4 的孤兒 process 清理有生效)。
> 3. **第二次**啟動 app,WiFi / U1 仍正常,但 **U2 連不上**。
> 4. 觀察到:第二次 U2 按下 START 時,**會重新開啟一個新的 adb.exe**,但這個新開的 adb.exe **最終會自己關閉**(而非正常持續運作),導致連線失敗。
>
> **請先加詳細診斷 log(不要直接修邏輯),涵蓋:**
> 1. **App 啟動時**:記錄目前 adb server 的偵測狀態(是否偵測到已有 server 在跑、`_server_owned_by_us` 之類的旗標當下是什麼值)。
> 2. **App 關閉/U2 停止時**:記錄清理流程執行了哪些步驟(是否呼叫 `adb kill-server`、清理前後的旗標狀態、adb.exe process 是否確實消失)。
> 3. **第二次 U2 啟動(connect())時**:記錄從「開始探測 adb server」到「adb devices/adb reverse 各步驟」的每一步狀態與耗時,包含新啟動的那個 adb.exe 的 PID、以及它「自己關閉」前後發生了什麼(是超時、是被誰終止、還是自然結束)。
> 4. 這些 log 要能讓我們回答:**第二次啟動時,程式是不是誤判了 adb server 的狀態?清理邏輯是否遺留了某個狀態旗標,導致第二次初始化用了錯誤的假設?**
>
> **只加診斷,不要動任何邏輯**,依 §10.4 說明加了哪些檔案的診斷 log,並告訴我怎麼重現(啟動→關閉→重啟→U2 連線失敗)並讀取 log。我會實測重現、把完整 log 貼給你,你再依真實數字判斷根因、提出修法讓我確認。

---
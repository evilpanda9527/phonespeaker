請處理 SPEC3.md §16 第 4 項,**只做這一項,不要動其他**。

**穩定重現條件(實測):**
- 連線、手機回 READY 後,在「尚未有任何 PCM 傳給手機」的空窗(例如系統當下無聲)按「停止」→ 整個 app 自動關閉。
- 若已傳過音訊再按停止,則正常(回到待機、可再啟動)。

**log 佐證:**
- 正常(有音訊)停止:`送出 PCM 失敗,判定為斷線` → `已還原 PC 預設輸出靜音狀態` → 回到「等待 WiFi client 連線…」,app 存活。
- 異常(無音訊)停止:只有 `已還原 PC 預設輸出靜音狀態` 一行,然後 app 直接結束,**沒有**「判定斷線 → 重新等待」那段。

**根因判斷:** 正常的「停止」是靠串流迴圈「送 PCM 失敗 → 判定斷線」這條路回到待機。若連一筆 PCM 都還沒送過就按停止,串流迴圈不會觸發那條路徑,停止流程因而走進另一條收尾路徑(很可能是 `os._exit()`),把整個 app 關掉。

**要求:**
1. 「停止」不論有沒有傳過 PCM,都必須回到「待機、可再按啟動」狀態,**絕不關閉整個 app**。
2. `os._exit()`(先前為繞過 pyaudiowpatch 關閉時 crash 而加的)**只能**屬於「關閉整個 app / 關閉視窗」路徑,不可被「停止串流」路徑觸發到。請確認「停止」與「關閉 app」這兩條路徑是分開的、不會混用同一個收尾函式。
3. 修正後,「停止」在以下兩種情況都要正常回到待機:(a) 已傳過音訊、(b) 連線 READY 但尚未傳任何 PCM。
4. **修改僅限 PC 端停止/收尾邏輯(`stream_engine.py` / `gui.py` 或相關檔案)**,不得動協定層(Handshake/TcpClient/AudioFormat)、Android 端、`WifiTransport`、`AudioPlayer`。
5. 依 §10.4 回報:改了哪些檔案、確認未動到已通過驗收的部分。
6. 修正後告訴我如何驗證這兩種情況。

**驗收會這樣測:** 連線後 (a) 不播任何聲音直接按停止 → app 應存活、回到等待連線、可再啟動;(b) 播放音訊後按停止 → 一樣正常。兩種都要過。

---
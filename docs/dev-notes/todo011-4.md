> 修正方向:**不要選項 1(到 READY 就停)那種折衷,改成更徹底的做法——U2 頁面在使用者按 START 之前完全 standby,不主動做任何背景 adb 輪詢/呼叫。**所有「裝置有沒有連上、USB 偵錯有沒有開、有沒有授權」的判斷,全部挪到使用者**按下 START 的那一刻**,由 `connect()` 一次性完成判斷並回報結果(找不到裝置 / unauthorized / 正常連線,都在這一次呼叫裡處理)。
>
> 這樣做的好處:徹底消除背景輪詢和 connect()/disconnect() 互撞的可能性(比「到 READY 就停」更乾淨,連 READY 之前的殘餘輪詢風險窗口也一併拿掉)。使用者體驗上的取捨:按下 START 後,可能要等 1~3 秒才會判斷完成、開始連線出聲——這個延遲可接受。
>
> 請依這個方向重新設計(移除背景輪詢機制,把狀態判斷邏輯整合進 connect() 的開頭),鎖(Lock)仍然保留作為額外的安全網(保護 disconnect() 被兩個執行緒同時進入那個風險點)。依你的規則 bump 版本號並跟我確認;依 §10.4 回報改了哪些檔案,並給我可驗證的測試步驟。

---
> 補充一條通用原則,請確認並記錄下來(可以記進 SPEC3 或專案規範):**standby 狀態下不主動做任何背景動作,只有使用者按下 START 才開始動作;按 STOP 或關閉 app,要乾淨、徹底地結束/收掉所有東西,不留殘餘。**
>
> **請先確認:WiFi 和 U1 現有設計本來就符合這條原則嗎?**(WiFi 本來就是 START 才廣播/監聽、STOP 就停止,沒有背景輪詢;U1 如果目前的前置偵測是做成持續輪詢,也要一併檢查是否符合。)如果本來就符合,**不需要為了這個原則去動 WiFi/U1 已驗收的程式碼**——只需要確認、不用修改。
>
> **真正需要改的,還是只有 U2**——就是我們前面談的:移除背景輪詢,改成 START 按下才做一次性判斷。這條原則是這次 U2 修法的依據,不是要你去重寫其他傳輸方式。
>
> 請先回報:WiFi/U1 現況是否已符合此原則(不用改,只需確認);U2 依此原則的修法照前一輪討論的方向進行。

---
**Prompt(§16-4 問題 A 停止逾時,仍未解決,請再修)**

> 破音(問題 B)已修好、停止不關 app 也維持正常,但**問題 A(停止逾時)仍未解決**:每次按停止,log 仍出現 `停止逾時:背景執行緒尚未結束`,且會卡約 3~4 秒才印出。上次的修法(擷取端 `stop_stream()`、transport 端先 shutdown 再進鎖)沒有真正讓卡住的執行緒及時結束。
>
> **請進一步診斷:到底是哪一個背景執行緒沒結束?** 建議在停止流程加入診斷:停止時列出仍存活的執行緒名稱(例如 `threading.enumerate()` 或對每個 join 逾時的執行緒個別記 log),先**確定是「音訊擷取執行緒」還是「transport 收送執行緒」卡住**,再對症下藥,不要再憑推測改。
>
> **要求:**
> 1. 先在 log 明確指出停止時是哪個(些)執行緒沒結束,把這個資訊也讓我在 app/powershell log 看得到。
> 2. 針對真正卡住的那個執行緒,確保停止時能主動中斷它的阻塞(擷取:確認 `stop_stream()` 真的讓 `read()` 返回;收送:確認 `shutdown` 真的讓 `recv()`/`sendall()` 返回;必要時檢查是否有 join 的逾時設定太短、或根本 join 錯了執行緒)。
> 3. **不得再影響串流播放品質**(破音問題 B 已修好,不可退回)。
> 4. 修改僅限 PC 端,不動協定層、Android 端、AudioPlayer。
> 5. 依 §10.4 回報,並告訴我如何驗證停止時不再逾時、且 log 會顯示執行緒診斷資訊。
>
> 先讓「哪個執行緒卡住」變成 log 上看得到的事實,再修。

---
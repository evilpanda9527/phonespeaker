**Prompt ①(先做這個,較單純):移除失效的靜音開關**

> M1-A WiFi 已通過驗收。請處理 SPEC3.md §16 第 2 項,**只做這一項,不要動其他**。
>
> **背景**:PC 端 GUI 上「連線時 PC 同時出聲(不自動靜音)」這個開關實測無作用——勾選後 PC 仍不出聲。使用者決定不修復功能,直接移除這個開關;需求簡化為「連線時 PC 一律靜音、只有手機出聲」。
>
> **要求**:
> 1. `gui.py`:移除該勾選方塊,以及它覆寫 `AUTO_MUTE_ON_READY_DEFAULT` 的相關邏輯。
> 2. `config.py`:讓「連線即靜音」成為固定行為(可移除 `AUTO_MUTE_ON_READY_DEFAULT` 這個可調項,或固定為 True)。
> 3. **必須保留斷線還原邏輯**——斷線/關 app 時 PC 音量自動復原(此功能實測正常,不可弄壞)。
> 4. **不得修改** `mute_control.py` 的還原行為、協定層、Android 端、`WifiTransport`、`AudioPlayer`。
> 5. 依 §10.4 回報:改了哪些檔案、確認未動到已通過驗收的部分。
>
> 修改後告訴我如何重新啟動 PC app 驗證(確認開關消失、連線仍自動靜音、斷線仍自動還原)。

---
